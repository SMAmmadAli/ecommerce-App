import 'package:flutter/material.dart';

class myMakeUp extends StatelessWidget {
  const myMakeUp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("MakeUp page"),
    );
  }
}
